using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Linq;
using System.Security.Policy;
using System.Text;
using System.Threading.Tasks;

namespace Ex1
{/*
    class ReaderThread : BackgroundWorker
    {
        Object lockObj { get; set; }
        String filePath { get; set; }
        bool isThread1Paused { get; set; }

        public ReaderThread(Object lockObj, String path)
        {
            this.lockObj = lockObj;
            this.DoWork += Read;
            this.filePath = path;
        }

        public void Read(object sender, DoWorkEventArgs e)
        {
            using (StreamReader sr = File.OpenText(filePath))
            {
                ыr.ReadToEnd().Split().ToList();
            }
            th
        }
    }*/
}
